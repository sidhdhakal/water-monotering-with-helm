apiVersion: v1
kind: Service
metadata:
  name: {{ include "heml-app.fullname" . }}
  labels:
    {{- include "heml-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heml-app.selectorLabels" . | nindent 4 }}
